# =============================================================================
# HOME LAB SERVER - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and update the values
# cp .env.example .env && nano .env
# =============================================================================

# -----------------------------------------------------------------------------
# DOMAIN CONFIGURATION (for Traefik routing)
# -----------------------------------------------------------------------------
# Your local domain (used for DNS routing via AdGuard)
# Example: myserver.lan -> images.myserver.lan
DOMAIN=myserver.lan

# -----------------------------------------------------------------------------
# IMMICH CONFIGURATION
# -----------------------------------------------------------------------------
# Location where your uploaded photos/videos are stored
# Use absolute path for reliability
UPLOAD_LOCATION=/home/${USER}/docker_data/immich

# Location where database files are stored
# IMPORTANT: Network shares are NOT supported for the database
DB_DATA_LOCATION=/home/${USER}/docker_data/postgres

# Immich version - use 'release' for latest stable, or pin like 'v1.99.0'
IMMICH_VERSION=release

# Timezone (optional) - uncomment and set your timezone
# TZ=Europe/London
# Full list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Connection password for PostgreSQL
# IMPORTANT: Use only A-Za-z0-9 characters (no special chars or spaces)
# Generate a random one with: openssl rand -base64 16 | tr -dc 'A-Za-z0-9'
DB_PASSWORD=CHANGE_ME_use_strong_password

# These usually don't need to be changed
DB_USERNAME=postgres
DB_DATABASE_NAME=immich
